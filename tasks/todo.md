# Current checklist

## Theory

- [ ] Derive the entrance-section contrast flow as a function of normalized
  initialization direction and conserved balance tensors.
- [ ] State the weakest observable orientation/access certificate that excludes both
  verified wrong basins; prove it or construct a new counterexample.
- [ ] Classify all remaining finite and compactified access-collapse strata.
- [ ] Close the success/failure partition up to a null remainder.

## Experiment

- [x] Freeze the public-compatible M1 MQAR law and standard Transformer.
- [x] Complete 20 paired seeds for standard, $2^{-8}$ Q/K, and exact-zero Q/K arms.
- [x] Reconstruct all seed artifacts, run 20,000 whole-seed bootstrap intervals, and
  record the direct-path measurement boundary.
- [x] Run the frozen signed-orientation M2 test and reconcile it with the verified
  single-layer failure basins.
- [x] Reject raw $K(0)=\pm Q(0)$ sign as a standalone standard-architecture boundary;
  retain data-defined kernel orientation plus factor access as the theorem object.

## Locked

- [ ] LEGO one-step training and depth composition.
- [ ] GPT-2 or larger open-model experiments.
- [ ] New task families not required by the active theorem.
