# Generated evidence is content-addressed by its manifests.  Suppress line-oriented
# diffs for CSV/SVG/JSON bundles so CRLF CSV records and SVG path whitespace do not
# create false whitespace failures; the files remain ordinary readable repository data.
results/** -diff
