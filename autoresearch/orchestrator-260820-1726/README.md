# Phase-II autoresearch ledger

Each accepted or discarded experiment records the frozen hypothesis/config, paired
streams, working and held-out signals, keep/discard/remedy decision, and inference class.
The pinned success predicate is in `orchestrator-state.json`; publication alone is not
success and must follow independent verification.

## Cycle 1

- The corrected baseline predicate passed all 137 published Phase-I tests.
- Phase-II mathematical/API contracts now cover codebook feasibility, composite-rank
  confounds, exact per-slot causal routing, nonlinear finite suffixes, full-population
  risk, bitwise schedule continuation, and an offline GPT-NeoX bridge.
- Discovery seed 100 is a speed/learning calibration only: at the hard architecture,
  step 800 held-out risk was 0.011948 and accuracy was 0.992920. It is not part of
  the untouched confirmation cohort.
- Units remain at four because no production result marker or final report is claimed
  before the controlled matrix and Pythia pilot actually run.
